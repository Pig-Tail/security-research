# runtime artifacts produced by the PoCs
**/marker.log
# local checkouts the PoCs run against (not redistributed)
**/webpack-dev-server/
**/node_modules/
**/dist/
# PoC build artifacts (downloaded jars / compiled classes)
**/poc/lib/
**/*.class
# generator intermediate
_index_rows.md

# embargoed staging folder — do NOT publish until advisory public
GHSA-q366-cv5v-83w8-openemr/
